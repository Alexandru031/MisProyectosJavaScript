/* Pide la edad y si es mayor de 18 años indica que ya puede conducir. */

let edad = prompt("Cuantos años tienes?")

let texto = edad > 18 ? "Puede conducir" : "No puede conducir"

console.log(document.write(texto)) /* "document.write() o innerHTML" - Sirve para escribir el texto en el documento en HTML */